import { Box, styled } from '@ignite-ui/react'

export const CalendarStepContainer = styled(Box, {
  margin: '$6 auto 0',
  padding: 0,
  maxWidth: '100%',
  display: 'grid',
  position: 'relative',
})
